// Placeholder script for fase4 (em desenvolvimento)
document.addEventListener('DOMContentLoaded', () => {
  const devNotice = document.querySelector('.quiz-card');
  if (devNotice) {
    // no interactive logic for now
    devNotice.setAttribute('aria-live', 'polite');
  }
});
