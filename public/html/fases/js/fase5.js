// Placeholder script for fase5 (locked / coming soon)
document.addEventListener('DOMContentLoaded', () => {
  const el = document.querySelector('.quiz-card');
  if (el) el.setAttribute('aria-hidden', 'true');
});
